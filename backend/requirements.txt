fastapi
uvicorn[standard]
duckdb
requests>=2.34.2
pydantic

# Transitive pins to clear known CVEs (pip-audit) in versions fastapi/uvicorn
# would otherwise resolve to.
starlette>=1.6.0
anyio>=4.15.1
click>=8.5.0
python-dotenv>=1.2.3
urllib3>=2.8.0
