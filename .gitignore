# Secrets — never commit the FRED API key
.env

# Python
.venv/
.venv-backend/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Raw landing zone for assessor pulls (large, re-creatable)
etl/raw/
*.duckdb.wal

# The database lives on the `data` release, not in git: python -m etl.db_store pull
etl/*.duckdb
etl/*.duckdb.*

# OS / editor
.DS_Store

# Node
frontend/node_modules/
frontend/dist/

# Static export (regenerated from the DuckDB snapshot by the deploy workflow)
frontend/public/data/
